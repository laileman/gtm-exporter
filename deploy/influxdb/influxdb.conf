# 基本配置
bind-address = "0.0.0.0:8086"

# 存储配置
[storage]
  engine = "tsm1"
  wal-dir = "/var/lib/influxdb2/wal"
  tsm-path = "/var/lib/influxdb2/engine/data"

# 认证配置
[http]
  auth-enabled = true

# 日志配置
[log]
  level = "info"
  format = "text"